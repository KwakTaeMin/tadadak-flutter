name: tadadak
description: chat-front
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

# pubspec.yaml 파일의 flutter 섹션에 presents-material-design: true 항목이 있는지 확인하십시오.
# 사전 정의된 material 아이콘 세트를 사용할 수 있습니다. 재료 라이브러리를 사용하는 경우 일반적으로 이 줄을 포함하는 것이 좋습니다.